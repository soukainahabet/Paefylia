server:
  port: 8081

# --------------------- JWT SECURITY ----------------------
jwt:
  secret: ${JWT_SECRET:votre-cle-secrete-256-bits-minimum-a-changer-en-production}
  expiration: 86400000

spring:
  application:
    name: demo

  # ---------------------- MYSQL ----------------------
  datasource:
    url: jdbc:mysql://localhost:3306/parfylia
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  # ---------------------- MONGODB ----------------------
  data:
    mongodb:
      host: localhost
      port: 27017
      database: parfylia_db

  # ---------------------- Neo4j ----------------------
  neo4j:
    uri: neo4j://127.0.0.1:7687
    authentication:
      username: neo4j
      password: Neo4j123
